version: '3.8'

services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: postgres_agent_attendance
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: postgres_agent_attendance
    ports:
      - "5432:5432"
    restart: unless-stopped

  redis:
    image: redis:7
    container_name: redis_agent_attendance
    ports:
      - "6379:6379"
    command: >
      redis-server
      --appendonly yes
      --maxmemory-policy noeviction
    restart: unless-stopped